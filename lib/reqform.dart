import 'package:flutter/material.dart';

class Reqform extends StatelessWidget {
  const Reqform({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Request Form"),
      ),
    );
  }
}
